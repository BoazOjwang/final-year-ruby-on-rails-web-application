<%= render 'home/header' %>
<section id="hero">
  <div id="heroCarousel" data-bs-interval="5000" class="carousel slide carousel-fade" data-bs-ride="carousel">
    <ol class="carousel-indicators" id="hero-carousel-indicators"></ol>
    <div class="carousel-inner" role="listbox">
      <div class="carousel-item active" style="background-image: url(<%= asset_path('firstImage') %>">
        <div class="container">
          <h2>Welcome to <span>DrugNet</span></h2>
          <p>We are health clinic based in Mathare North area, Ruaraka. We consist of Doctors who work in government public hospitals and have formed a team of Doctors having our own private practice when we are not saving lives in the government hospitals.</p>
          <a href="#about" class="btn-get-started scrollto">Read More</a></div>
      </div>
      <div class="carousel-item" style="background-image: url(<%= asset_path('slide-2') %>">
        <div class="container">
          <h2>Mobile Health</h2>
          <p>We are health clinic based in Mathare North area, Ruaraka. We consist of Doctors who work in government public hospitals and have formed a team of Doctors having our own private practice when we are not saving lives in the government hospitals.</p>
          <a href="#about" class="btn-get-started scrollto">Read More</a></div>
      </div>
      <div class="carousel-item" style="background-image: url(<%= asset_path('slide-3') %>">
        <div class="container">
          <h2>Mobile Health</h2>
          <p>We are health clinic based in Mathare North area, Ruaraka. We consist of Doctors who work in government public hospitals and have formed a team of Doctors having our own private practice when we are not saving lives in the government hospitals.</p>
          <a href="#about" class="btn-get-started scrollto">Read More</a></div>
      </div>
    </div>
    <a class="carousel-control-prev" href="#heroCarousel" role="button" data-bs-slide="prev"> <span class="carousel-control-prev-icon bi bi-chevron-left" aria-hidden="true"></span> </a> <a class="carousel-control-next" href="#heroCarousel" role="button" data-bs-slide="next"> <span class="carousel-control-next-icon bi bi-chevron-right" aria-hidden="true"></span> </a></div>
</section>

<main id="main">
  <%= render 'home/services' %>
  <%= render 'home/about' %>
  <%= render 'home/features' %>
  <%= render 'home/testimonials' %>
  <%= render 'home/staff' %>
  <%= render 'home/contact' %>
  <%= render 'home/footer' %>
</main>

<%# <div id="preloader"></div> %>
